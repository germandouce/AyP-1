//Escribir una función int area_rectangulo(int base, int altura) que permita calcular el área de un
// rectángulo dada su base y altura.

#include <stdio.h>

// DOC: Completar
int area_rectangulo(int base, int altura) {
    int area =  base * altura;
    return 0;
}

int main() {
    int base = 4;
    int altura =5;
    area_rectangulo(base, altura);
    return 0;
}
